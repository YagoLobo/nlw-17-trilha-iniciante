// arrays, objetos

let meta = {
    value: 'ler um livro por mes',
    checked: false,
    log: (info) => {
        console.log(info)
    }
}

meta.value = "Nao e mais ler um livro"
meta.log(meta.value)

// arrow function
//const criarMeta = ( ) => {}

//function
function criarMeta() {}